package no.valg.eva.admin.common.configuration.model.election;

/**
 * D = direkte valg, F = forholdstallsvalg og R = folkeavstemning (referendum).
 */
public enum GenericElectionType {
    D, F, R;
}

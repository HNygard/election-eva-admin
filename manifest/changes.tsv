# Every deviation from the EVA Admin 2019 release, declared.
#
# Format, tab separated:
#   STATUS<TAB>PATH<TAB>MILESTONE<TAB>REASON
#
# STATUS
#   MODIFIED    a release file whose content differs from manifest/originals.sha
#   DELETED     a release file that is no longer present
#   ADDED       a file that is not part of the release
#   GENERATED   an added file produced by a tool, regenerable, not hand-edited
#   ADDED_TREE  a directory of our own scaffolding; every file under it counts as
#               ADDED. Rejected if it overlaps any release path, so it can never
#               be used to hide an edit to EVA Admin source.
#
# MILESTONE is M0 (setup), M1..M4, or the milestone the change belongs to.
# REASON must say why, not what. "Fixed web.xml" is not a reason.
#
# Verified by tools/manifest-check.sh, which runs as a pre-commit hook.
# See docs/decisions/D002-changes-in-place-with-manifest.md

MODIFIED	readme.md	M0	Repo owner recorded which modules are missing from the release, before this control system existed. Baseline for this file is therefore the untouched 2017 import.

ADDED	added-stuff/README.md	M0	2019-2020 notes from the first attempt to build and deploy: docker commands tried and the errors they produced. Kept as evidence, superseded by tools/ and docs/findings/.
ADDED	added-stuff/jboss/Dockerfile	M0	WildFly 17 image built during that first attempt.
ADDED	added-stuff/jboss/start.sh	M0	Deployment script from that first attempt; copies the built WARs into WildFly.

ADDED	.gitignore	M0	The release shipped without one. Keeps build output and bulky reference material out of the manifest check.
ADDED	CLAUDE.md	M0	Operating instructions for AI agents working this repo.

ADDED_TREE	manifest	M0	The control that makes in-place changes traceable: baseline definition, generated checksums, and this declaration file.
ADDED_TREE	tools	M0	Dockerised build toolchain, manifest and docs checks, git hook. Nothing here touches EVA Admin source.
ADDED_TREE	docs	M0	Status, decisions, findings, work queue, specs and reference index.
ADDED_TREE	docker	M0	PostgreSQL on the coordinates the release expects, for M2 onwards.

GENERATED	docker/postgres/initdb/01-schema-generated.sql	M2	PostgreSQL DDL derived from the release's 120 @Entity classes by tools/schema-export. Regenerate with tools/schema-export/generate.sh; never hand-edit. The release ships no schema and the Flyway migrations were withheld (NF-007).

# --- M2: making admin-backend deployable -------------------------------------
MODIFIED	admin/admin-backend/src/main/webapp/WEB-INF/web.xml	M2	Released as a single comment line with no root element, so WildFly fails the deployment at PARSE with "Unexpected EOF in prolog". Replaced by an empty web-app 3.1 shell: this WAR holds EJBs and no servlets of its own. Same shell that made the backend start on origin/attempt. NF-004.

GENERATED	admin/admin-backend/src/main/resources/META-INF/persistence.xml	M2	The runtime persistence unit the release does not ship; all nine of its persistence.xml files are test-scoped. Without it: "Can't find a persistence unit named 'evotePU'". Unit name and java:/jdbc/evote both recovered from the release source, entity list derived from the 120 @Entity classes. Regenerate with tools/generate-persistence-xml.sh. NF-006.

ADDED	admin/admin-backend/src/main/webapp/WEB-INF/jboss-deployment-structure.xml	M2	WildFly exposes only javax.persistence to a deployment, but EVA Admin's repositories use org.hibernate.Session and org.hibernate.jdbc.Work directly, so Weld refused to define the entire repository layer as beans. Adds the org.hibernate module dependency. NF-017.

ADDED	admin/admin-backend/src/main/resources/log4j.xml	M2	Log4jUtil.configureFromClasspathConfig looks up log4j.xml on the classpath and throws "Unable to find log4j.xml on classpath" without it, taking down Log4jConfigurator. Copied verbatim from the release's own admin-backend test resources. NF-018.
ADDED	admin/admin-backend/src/main/resources/log4j.dtd	M2	Referenced by the DOCTYPE of the log4j.xml above; copied from the same place in the release. NF-018.
ADDED	admin/admin-backend/src/main/resources/version.properties	M2	CommitIdProducer reads /version.properties at startup and throws without it. Not invented: admin-backend/pom.xml runs git-commit-id-plugin and filters src/main/resources, so the release had this file and it was stripped. Uses the plugin's own ${git.commit.id}. NF-018.

# beans.xml, one per bean archive. The release ships none, leaving every archive
# an implicit bean archive, which under CDI 1.1 only discovers classes with a
# bean-defining annotation -- and most EVA Admin services have none. See D010.
ADDED	admin/admin-common/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-integration/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-voting/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-backend-common/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-configuration/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-counting/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-rbac/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-settlement/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-valgnatt/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-rapport/src/main/resources/META-INF/beans.xml	M2	bean-discovery-mode=all, so constructor-injected services are discovered. NF-005.
ADDED	admin/admin-backend/src/main/webapp/WEB-INF/beans.xml	M2	bean-discovery-mode=all for the backend WAR itself. NF-005.
ADDED	admin/admin-frontend/src/main/webapp/WEB-INF/beans.xml	M2	bean-discovery-mode=all for the frontend WAR, needed once the frontend deploys in M3. NF-005.

# --- M3: making admin-frontend deployable ------------------------------------
MODIFIED	admin/admin-frontend/src/main/webapp/WEB-INF/web.xml	M3	Released as a single comment line with no root element. Reconstructed: 10 filters and listeners carry no annotations and exist only if declared here, plus the ${session.cookie.secure} and ${jsf.projectStage} placeholders the root pom's war-plugin profiles filter. Filter ORDER is partly derived from UserData data flow and partly assumed; see NF-015 before trusting it. NF-004.
ADDED	admin/admin-frontend/src/main/resources/META-INF/faces-config.xml	M3	Registers MultiPageMessagesSupport, a PhaseListener that can only be declared in faces-config.xml. Everything else in the frontend is annotation registered, so this file contains nothing else. Without it, faces messages vanish across redirects. NF-016.
ADDED	admin/admin-frontend/src/main/webapp/WEB-INF/jboss-deployment-structure.xml	M3	Same org.hibernate module visibility the backend needs; the frontend shares admin-common, whose repositories use the Hibernate API directly. NF-017.

# --- M3: getting past the gates ----------------------------------------------
ADDED	admin/admin-backend/src/main/java/no/valg/eva/admin/reconstruction/SystemPassphraseAutoUnlock.java	M3	DISABLES A SECURITY FEATURE. Sets the system passphrase at startup because nothing in the published source can enter it: the passphrase is in memory so SQL cannot seed it, and the remote interface exposes only isPasswordSet(). In production this passphrase unlocks EVA's signing material and an operator entering it is what authorises the deployment. Harmless only while the database holds no signing keys. Package name marks it as not-EVA. Disable with EVA_AUTO_UNLOCK=false. See D011, NF-021.
ADDED	admin/admin-frontend/src/main/resources/no/valg/eva/admin/frontend/util/version.properties	M3	Frontend half of the version pair. VersionProperties loads it package-relative rather than from the classpath root. LifecycleFilter disables the whole application when frontend-version and backend-version differ, so both must exist and agree. NF-018.
GENERATED	admin/admin-common/src/main/resources-unfiltered/no/valg/eva/admin/common/Messages_nb_NO.properties	M3	The i18n bundle the release does not ship; without it every JSF page dies with MissingResourceException before rendering. 1386 keys recovered from the XHTML and Java that reference them; values are PLACEHOLDERS derived from the keys, since the real Norwegian text was never published and lives per election event in the database. Regenerate with tools/generate-message-bundle.sh. NF-022.
GENERATED	admin/admin-common/src/main/resources-unfiltered/no/valg/eva/admin/common/Messages_nn_NO.properties	M3	Nynorsk half of the same generated bundle, same placeholder caveat. NF-022.
